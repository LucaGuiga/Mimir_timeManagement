// Copy to src/config.js (gitignored). API_BASE is the FastAPI origin; leave "" when the
// dashboard is served by the same uvicorn process. API_TOKEN must equal api_token in api/config.yaml.
export const API_BASE = '';
export const API_TOKEN = 'change-me';
